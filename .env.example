# Public browser configuration. This is an origin, for example https://api.example.com.
VITE_API_URL=

# Used only by the Worker, never by Vite or the browser.
# Format: salt:iterations:lowercase-pbkdf2-sha256-hex
ADMIN_PASSWORD_HASH=
FRONTEND_ORIGIN=https://advaitachandra.in